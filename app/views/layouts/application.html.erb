<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">

		<!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame
		Remove this if you use the .htaccess -->
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title>Orasi Coding Challenge</title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<%= stylesheet_link_tag "bootstrap.css" %>
		<%= javascript_include_tag "application.js"%>
		<!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
		<!--[if lt IE 9]>
		  <script src="/assets/html5shiv.js"></script>
		  <script src="/assets/respond.min.js"></script>
		<![endif]-->
	</head>

	<body>
		<header>
			<div class="container">
				<div class="navbar">
					<%= link_to root_path do %>
					<%= image_tag "Logo.png", width: "50px", height: "45px"%>
					<% end %>
					<ul class="nav nav-pills pull-right">
						<% if current_user.nil? %>
						<li>
							<%= link_to "Login", login_path %>
						</li>
						<% else %>
						<% if current_user.admin %>
						<li>
							<%= link_to "Admin", :admin %>
						</li>
						<% end %>
						<li>
							<%= link_to "Bronze", october_bronze_path%>
						</li>
						<li>
							<%= link_to "Silver", october_silver_path%>
						</li>
						<li>
							<%= link_to "Gold", october_gold_path %>
						</li>

						<li>
							<%= link_to "Logout", logout_path %>
						</li>
						<li>
							<p>
								Welcome, <%= link_to current_user.first_name.capitalize + ' ' + current_user.last_name.capitalize, profile_view_path %>
							</p>
						</li>
						<li>
							<!-- Gravatar stuff blank will be displayed if they don't have a gravatar. -->
							<%= link_to image_tag("http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(current_user.username)}?s=50&r=g&d=mm"), "http://en.gravatar.com/", options={:confirm => "This will bring you to Gravatar.  Are you sure you would like to leave?", :style=>"padding:0px"} %>
						</li>
						<% end %>
					</ul>
				</div>
			</div>
		</header>

		<div class="container">
			<%= yield %>
		</div>

	</body>
</html>
